# write a program that accept radius from user and print area of circle

from math import pi
r = float(input("Input the radius of circle:-  "))
p = F"The area of the circle with radius {str(r)}  is: {str(pi*r**2)}" 
print(p)
